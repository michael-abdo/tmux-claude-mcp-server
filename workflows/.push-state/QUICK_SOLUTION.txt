🚀 QUICKEST SOLUTION:

1. Visit this URL in your browser:
   https://github.com/michael-abdo/tmux-claude-mcp-server/security/secret-scanning/unblock-secret/2zIv1tEDuZkrZBAkAQ9oe2vMFps

2. Click 'Allow secret' or 'Bypass protection'

3. Then run:
   git push origin HEAD:master

✅ This will push your code immediately without removing the secrets.

📝 Note: workflow_state/ is already in .gitignore, so no new secrets will be committed.
